

var num = 6;
//var num = 6;
var num = -500;
//var num = 70;


/*when both numbers are equal*/
if (num == 6) {
  alert ("Lilies have 6 petals")
}

/*when the number is less than 6*/
else if (num < 6) {
  alert ("This number is too small")
}

/*when the numbers are not less than or equal to 6*/
else {
  alert ("Try Again")
}
